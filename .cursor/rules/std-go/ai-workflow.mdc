---
alwaysApply: false
description: AI 生成 Go 服务端代码的基本要求
---

# AI 生成 Go 服务端代码的基本要求

> 本文档基于 Effective Go、Go Proverbs 和业界最佳实践。框架无关，专注于 Go 语言核心实践。通用原则请参考 `ai-workflow.mdc`（SOLID、KISS、DRY、YAGNI）

---

## 适用范围与规则选择

<scope>
本规则包含两类规则：

1. **通用规则（所有项目必须遵循）**：错误处理、context、并发安全、命名规范、依赖注入
2. **场景规则（根据项目类型选择）**：分层架构、请求验证、健康检查等

| 项目类型 | 适用章节 | 可跳过章节 |
|---------|---------|-----------|
| HTTP API / gRPC 服务 | 全部 | - |
| Kubernetes Operator | 通用规则 + 并发 | 分层架构、请求验证、健康检查 |
| CLI 工具 | 通用规则 | 分层架构、请求验证、健康检查 |
| 后台 Worker | 通用规则 + 并发 + 优雅关闭 | 分层架构、请求验证 |
| 大型开源项目 | **项目规范优先** | 根据项目规范决定 |

**重要**：对于有 CONTRIBUTING.md 或代码规范的项目，应优先遵循项目已有规范。
</scope>

---

## 工作流

1. **分析**：理解意图，识别项目类型和约束
2. **设计**：根据项目类型选择合适的架构模式
3. **实现**：遵循 Go 惯用法，确保错误处理、并发安全与可测试性
4. **验证**：根据仓库实际配置，运行对应的验证命令
5. **自查**：输出代码审核指南

---

## 分层架构（API 服务推荐）

| 层 | 职责 |
|-----|------|
| Handler | 处理协议层细节（HTTP/gRPC）、请求验证、响应转换 |
| Service | 业务逻辑、编排调用 |
| Repository | 数据访问、封装存储细节 |

---

## 错误处理（Error Handling）

| 规则 | 说明 |
|-----|------|
| **禁止静默失败** | 不要 `_ = err` 或空 return |
| **错误要有上下文** | 用 `fmt.Errorf("xxx: %w", err)` 包装 |
| **判断用 errors.Is/As** | 禁止字符串比较 |

```go
// ✅ 错误包装
return nil, fmt.Errorf("find user %s: %w", id, err)

// ✅ 错误判断
if errors.Is(err, ErrUserNotFound) { ... }
var valErr *ValidationError
if errors.As(err, &valErr) { ... }

// ❌ 禁止
if err.Error() == "not found" { }  // 字符串比较
```

---

## Context 使用规范

| 规则 | 说明 |
|-----|------|
| **第一个参数** | ctx 作为函数第一个参数 |
| **必须传递** | 所有跨边界调用（DB/HTTP/RPC）必须传 ctx |
| **设置超时** | 对外部依赖必须设置超时 |
| **不要存储** | 不要把 ctx 存在结构体中 |

```go
// ✅ 设置超时
ctx, cancel := context.WithTimeout(ctx, 3*time.Second)
defer cancel()
return s.repo.FindByID(ctx, id)

// ❌ 不传递 context / 存储 context
db.Query("SELECT...")              // 无法取消/超时
type BadService struct { ctx context.Context }  // 不要这样
```

---

## 接口设计

| 原则 | 说明 |
|-----|------|
| **小而精** | 1-3 个方法的接口最佳 |
| **使用方定义** | 接口由使用者定义 |
| **接受接口，返回具体** | 函数参数用接口，返回值用具体类型 |

---

## 依赖注入

构造函数注入依赖，main 函数组装（手动或 wire）。避免全局变量和 init() 初始化。

---

## 命名规范

- **包名**：小写单词，无下划线
- **缩写**：全大写或全小写（`HTTP`/`ID`/`url`）
- **接口**：动词+er（`Reader`, `UserFinder`）
- **避免 Stutter**：`user.Service` ✅，`user.UserService` ❌

---

## 完成验证

<iron_law>
**声称什么，就验证什么**。不要声称超出验证范围的结论。

| 变更类型 | 验证方式 |
|----------|----------|
| 核心逻辑/Bug修复 | 运行相关测试 |
| 一般代码修改 | 编译/vet |
| 配置/文档 | 人工审查 |

✅ "代码已修改，请运行测试验证" | ❌ "修改完成，测试应该能通过"
</iron_law>

---

## 代码审核指南（必须输出）

生成代码后必须输出审核指南，包含：
- **必须检查**：错误包装、context 超时、并发保护、SQL 参数化、输入验证
- **核心决策点**：错误策略、分层边界
- **边界处理**：nil/空切片/零值、超时/重试策略

