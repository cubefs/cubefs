---
alwaysApply: true
description: AI 生成代码的基本要求 - 通用编码原则和工作流,适用于所有技术栈
---

# AI 生成代码的基本要求

> 核心目标:生成可读、可维护、可测试的代码。AI 必须严格遵循本文档中的所有规则。

---

## 工作流 (Workflow)

在执行任何编码任务时,必须遵循以下思考过程:

1. **分析 (Analyze)**:理解用户意图,识别关键约束和边缘情况
2. **设计 (Design)**:规划模块结构、接口定义和数据流
3. **实现 (Implement)**:编写符合 SOLID 原则的代码,确保类型安全和错误处理
4. **验证 (Verify)**:根据仓库实际配置,运行对应的验证命令
5. **自查 (Review)**:对照代码审核指南进行自我审查

---

## 经典工程原则

### SOLID 原则

| 原则 | 要点 | 违反后果 |
|---|---|---|
| **S** 单一职责 | 一个类/函数只做一件事,只有一个修改理由 | 改一处牵动多处,测试困难 |
| **O** 开放封闭 | 对扩展开放,对修改封闭;用抽象和多态扩展功能 | 每次新增功能都要改原有代码 |
| **L** 里氏替换 | 子类可以替换父类而不影响程序正确性 | 继承体系脆弱,运行时出错 |
| **I** 接口隔离 | 接口要小而专一,客户端不应依赖它不需要的方法 | 被迫实现无用方法,耦合过重 |
| **D** 依赖倒置 | 高层模块依赖抽象接口,不依赖具体实现 | 底层变动导致高层连锁修改 |

### 其他核心原则

| 原则 | 要点 | 实践指导 |
|---|---|---|
| **KISS** | Keep It Simple - 保持简单 | 能用简单方案解决就不要过度设计;代码行数不是目标,清晰才是 |
| **DRY** | Don't Repeat Yourself | 相同逻辑出现两次以上必须提取;但不要为了 DRY 强行抽象不相关的代码 |
| **YAGNI** | You Aren't Gonna Need It | 只实现当前需要的功能;"以后可能用到"不是写代码的理由 |

---

## 代码设计规范

### 可读性优先

代码是写给人维护的,机器执行只是附带功能。

- **清晰 > 简洁**:当两者冲突时,选择清晰
- **显式优于隐式**:明确的输入输出契约,不依赖隐式类型转换或魔法行为
- **最小惊讶**:函数行为应符合命名暗示,无隐藏副作用(例如:名为 `getUser` 的函数不应该修改数据库)

### 函数设计

- **规模**:追求"一屏可见"(约 30-50 行),过长必须拆分
- **参数**:超过 3 个参数用对象封装;避免裸布尔参数,用命名参数或枚举
- **返回值**:类型可预期;查询类操作返回"空"表示(空数组/null/Option),不抛异常
- **纯函数优先**:相同输入 → 相同输出,无外部状态修改

### 关注点分离

数据获取、业务逻辑、展示渲染分层处理。不要把网络请求写在 UI 组件里,不要把业务规则写在数据访问层。

---

## 错误处理

### 基本原则

- **禁止静默失败**:空 catch 块是生产事故的根源
- **在合适层级处理**:底层不要吞掉错误返回 null,也不要把所有错误都抛到最顶层
- **策略一致**:异常或返回值二选一,确保调用方无法忽略错误
- **错误信息要有用**:必须包含足够的上下文来定位问题(包括关键参数、状态、操作等),避免"操作失败"这类无用信息

---

## 依赖与安全

### 依赖管理

- **依赖注入**:外部依赖(数据库、网络、文件系统)通过参数传入,这是可测试性的基础
- **禁止循环依赖**:模块依赖应该是单向、层次化的
- **最小化依赖**:每个外部依赖都是维护负担,引入前评估是否真的需要
- **参数化查询**:所有数据库查询必须使用参数化,防止 SQL 注入

### 安全要求

- **输入验证**:所有外部输入必须验证,使用白名单策略
- **敏感信息**:禁止硬编码密钥/密码;禁止在日志中输出敏感数据
- **资源管理**:使用语言提供的机制(try-finally/using/defer)确保资源释放
- **防御编程**:关键路径有超时、重试、降级策略

---

## 注释规范

### 核心原则

注释解释"为什么",代码本身表达"是什么"。

### 必须注释的场景

- 公开 API 的文档注释
- 复杂的业务逻辑或算法的原理说明
- 临时方案和技术债务(标注原因和改进计划)
- 非显而易见的性能优化(说明为什么这样做)

### 禁止的注释

- 复述代码内容的注释(代码能表达的不要用注释)
- 被注释掉的代码(用版本控制,不要留死代码)
- 过时的注释(错误的注释比没有更糟糕)

### TODO/FIXME 规范

临时代码和待办事项必须用标准标记,并包含日期和责任人。

```
// TODO(zhangsan, 2024-01-15): 性能优化 - 当前 O(n²),数据量大时需要改为 O(n log n)
// FIXME(lisi, 2024-01-10): 存在竞态条件,需要加锁
```

---

## 性能与测试

### 性能意识

- **不做过早优化**:先写正确、清晰的代码,只有性能确实成为问题时才优化
- **不写明显低效的代码**:循环内不做重复计算,大数据集考虑分页或流式处理
- **性能换可读性要注释**:如果优化导致代码难以理解,必须注释说明原因和收益

### 可测试性设计

- **设计时考虑**:难测试的代码通常意味着设计有问题
- **测试覆盖**:正常路径 + 边界条件 + 异常路径
- **测试独立**:每个测试用例独立运行,不依赖其他测试的执行顺序

---

## AI 生成规则

### 生成前

- **需求有歧义** → 先澄清再动手,不要猜测用户意图
- **多种实现方案** → 说明各自的取舍,让用户选择
- **存在风险或设计缺陷** → 必须提醒用户

### 生成时

- **保持一致性**:遵循项目已有的模式和代码风格
- **不生成死代码**:不生成被注释掉的代码块
- **不生成占位符**:如果必须用占位符,明确提醒需要替换
- **新依赖要说明**:引入新的库或依赖必须明确告知

### 生成后

- **解释设计决策**:说明关键的设计选择和权衡理由
- **说明假设**:如果有简化、省略或假设,必须说明
- **验证后再声称完成**:如需声称"测试通过"或"构建成功",必须先运行验证命令并看到实际输出
- **输出审核指南**:必须输出结构化的代码审核指南(格式见下文)

---

## 完成验证(Iron Law)

> 核心原则:**声称什么,就验证什么**。不要声称超出验证范围的结论。

### 验证分级

根据变更风险选择合适的验证强度,避免过度验证:

| 变更类型 | 验证方式 | 说明 |
|----------|----------|------|
| **核心逻辑/Bug修复** | 运行相关测试 | 根据仓库配置选择测试命令 |
| **一般代码修改** | 类型检查/Lint | 仓库有对应工具时才执行 |
| **配置/文档/样式** | 人工审查即可 | 无需运行命令 |
| **新增功能(有测试)** | 运行新增的测试 | 只跑相关测试,非全量 |

**关键原则**:
- **按需验证**:只使用仓库中已配置的工具,不强制要求特定工具
- 不需要每次都跑完整测试套件
- 验证范围匹配变更范围
- 声称"测试通过"时才需要跑测试;声称"代码已修改"则不需要

### 必须验证的场景

以下场景必须运行对应验证命令(使用仓库实际配置的工具):

| 声称 | 需要的验证 |
|------|------------|
| "测试通过" | 运行测试,看到 0 failures |
| "构建成功" | 运行构建,看到 exit 0 |
| "Bug 已修复" | 复现原始场景,确认问题不再出现 |
| "Lint 通过" | 运行 linter,看到 0 errors |

### 不需要验证的场景

以下场景人工审查即可,无需运行命令:

- 修改注释/文档/README
- 调整代码格式/变量命名
- 添加类型定义
- 修改配置文件(非关键配置)

### 红线警告

**禁止的模糊声称**:
- "测试应该能通过" → 要么跑测试,要么只说"代码已修改"
- "构建应该没问题" → 要么跑构建,要么不声称构建状态

**正确做法**:
```
✅ "代码已修改,请运行测试验证"(未验证时的诚实表述)
✅ [运行测试命令] [34/34 pass] "测试通过"(验证后的声称)
❌ "修改完成,测试应该能通过"(未验证却声称测试状态)
```

---

## 代码审核指南(必须输出)

代码生成完成后,必须输出结构化的审核指南,按高/中/低风险分类。

### 风险识别标准

**高风险项**(必须标注):
- 核心业务逻辑(金额、权限、状态流转)
- 数据变更操作(INSERT/UPDATE/DELETE)
- 外部系统调用(API、消息发送)
- 边界条件处理(空值、极值、并发)
- 安全敏感代码(认证、授权、加密)

**中风险项**(必须标注):
- 数据格式假设
- 业务规则假设
- 环境假设(Redis、网络等)
- 性能假设(数据量、并发量)
- 第三方行为假设

**低风险项**(可略过):
- 纯类型定义
- 常量配置
- 框架样板代码
- 工具函数
